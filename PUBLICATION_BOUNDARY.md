# V1.2 公开发布边界

GitHub 仓库是公开交付面，不等于本地完整数据仓库。公开内容由白名单脚本生成，不能直接把项目目录整体推送。

## 可以公开

- 产品说明、三步工作流和 V1.2 执行规范；
- 字段、指标、标签字典；
- 品牌、实体等不含账号登记信息的主数据；
- `mart_*` 汇总指标、已审核分析结论和 BI 管理工作簿；
- 数据质量、指标对账、冻结指纹和页面验收报告；
- V1.2 构建、工作簿、校验和公开包脚本；
- 运行网页所需的最小化页面数据。

## 不可以公开

- 原始抓取文件、候选列表、运行记录和完整正文；
- 完整内容事实表、互动逐条快照、关系明细和账号登记库；
- 本地路径、登录状态、密钥、Cookie 和临时访问参数；
- `90_历史归档` 与可重新生成的本地预览附件；
- 任何尚未完成脱敏或无法说明用途的文件。

## 两个公开面

- `main`：GitHub Pages，只包含 6 个网页运行文件。
- `project-source`：方法、字典、汇总表、工作簿、验收报告和脱敏页面源码。

发布统一运行 `99_工程工具/scripts/build_public_v1_2_release.mjs` 与 `build_public_source_v1_2.mjs`。脚本会阻止历史归档、原始层路径、本机绝对路径和临时访问参数进入公开包。
