# 公开同步边界

## 为什么不直接推送本地仓库

本地工程保留完整数据血缘和历史版本，其中包含只用于登录态回读的短时效查询参数；历史 Git 对象中也包含已归档文件。直接推送本地分支，即使删除当前目录里的归档，旧对象仍可能进入公开仓库历史。

因此 `project-source` 是从空历史创建的独立公开分支，只接收明确白名单中的文件。

## 公开内容

- 数据抓取的方法、来源职责和停止规则；
- 数据清洗的主题、标签与筛选规则；
- 经公开构建脚本脱敏后的网页源码和数据快照；
- 公开包构建与页面验收脚本。

## 不公开内容

- `90_历史归档/**`；
- Codex 原始抓取数据与内部运行记录；
- 含临时访问参数的来源登记、内容证据和线索文件；
- 原始 Excel、完整数据中心工作簿；
- `99_工程工具/outputs/**`、缓存、预览和本机状态；
- 本地完整仓库的历史提交。

公开链接统一使用不含查询参数的标准小红书帖子或主页地址。公开分支不得与本地完整历史 merge 或 cherry-pick；后续修改应从 `project-source` 分叉并回到该分支。
